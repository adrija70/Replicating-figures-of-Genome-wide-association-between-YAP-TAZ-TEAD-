
```r
plot(venn_data)
```

This calls:

- `plot()` method defined by **Vennerable**
- Draws a 2-set Venn diagram
- Area labels reflect the numbers you supplied

No genomic intelligence here. It’s just drawing.

---

## Mental model to keep

- `import()` → turns text files into **spatial genomic objects**
- `GRanges` → intervals that know how to overlap
- `subsetByOverlaps()` → spatial filtering
- `length()` → counts intervals
- `Venn()` → counts → geometry
- `plot()` → picture
